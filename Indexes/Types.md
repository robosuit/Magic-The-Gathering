# Type Index

## 🪲 Creatures

```dataview
LIST
FROM "Cards"
WHERE contains(tags, "#type/creature")
SORT file.name
```

---

## ⚡ Instants

```dataview
LIST
FROM "Cards"
WHERE contains(tags, "#type/instant")
SORT file.name
```

---

## 📜 Sorceries

```dataview
LIST
FROM "Cards"
WHERE contains(tags, "#type/sorcery")
SORT file.name
```

---

## 🏔️ Lands

```dataview
LIST
FROM "Cards"
WHERE contains(tags, "#type/land")
SORT file.name
```

---

## ⚙️ Artifacts

```dataview
LIST
FROM "Cards"
WHERE contains(tags, "#type/artifact")
SORT file.name
```

---

## 🛡️ Equipment

```dataview
LIST
FROM "Cards"
WHERE contains(tags, "#type/equipment")
SORT file.name
```

---

## ✨ Enchantments

```dataview
LIST
FROM "Cards"
WHERE contains(tags, "#type/enchantment")
SORT file.name
```

---

## 👻 Planeswalkers

```dataview
LIST
FROM "Cards"
WHERE contains(tags, "#type/planeswalker")
SORT file.name
```
